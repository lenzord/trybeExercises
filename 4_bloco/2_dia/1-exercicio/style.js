// CONSTRUÍNDO O ALGORÍTMO:

//1) Utilize a variável numeros;
//2) Percorra cada elemento do array(loop for);
//3) Imprima todos os valores nele contidos(console.log);

let numeros = [10, 52, 9, 12, 13, 70, 19, 24, 63, 99];

for(let index = 0; index < numeros.length; index += 1) {
    console.log(numeros[index]);
}